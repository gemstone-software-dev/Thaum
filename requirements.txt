# Same dependency set as pyproject.toml [project.dependencies] — change both when updating.
# Python >=3.11 (see pyproject.toml requires-python).
# gemstone_utils v0.4.x (UUID key ids, KEK canary on GemstoneKeyKdf). PyPI pin for reproducible installs.
# Local dev: pip install -e ../gemstone_utils
# Install from metadata: pip install .
# Optional Azure extras: pip install ".[azure]"
gemstone_utils==0.4.0
tzdata; platform_system == "Windows"
Flask
gunicorn
pydantic>=2.0
python-json-logger>=2.0.7
webexpythonsdk
SQLAlchemy
ldap3
psycopg[binary]>=3.2
